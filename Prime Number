#Prime numbers
#prime number is a natural number which is greater than 1 and divisible by 1,therefore only 1 can divide those numbers

number = int(input("Enter a number: "))

if number > 1:
    is_prime = True
    for i in range(2, int(number**0.5) + 1):
        if number % i == 0:
            is_prime = False
            break
    if is_prime:
        print(f"{number} is a prime number.")
    else:
        print(f"{number} is not a prime number.")
else:
    print(f"{number} is not a prime number.")

OUTPUT:
Enter a number: 7
7 is a prime number.
